{
  "name": "widget-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Flight Deck Widget Studio — visual widget-design IDE, no build step.",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "@playwright/test": "^1.55.0"
  }
}
