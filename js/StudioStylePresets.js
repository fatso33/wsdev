/**
 * StudioStylePresets.js
 * Re-exports the canonical style-preset catalog (shared/widgets/StylePresets.js,
 * synced to widgets/StylePresets.js via scripts/sync-shared.mjs) so existing
 * imports in this app don't need to change. Edit the shared copy, not this file.
 */
export { STYLE_PRESETS } from '../widgets/StylePresets.js';
